export interface MemberType {
  id: string;
  name: string;
  email: string;
  phone: string;
  address: string;
  createdAt: string;
  updatedAt: string;
}

export interface BooksType {
  id: string;
  title: string;
  author: string;
  publisher: string;
  status: boolean;
}
